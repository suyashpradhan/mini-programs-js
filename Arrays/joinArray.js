//! Write a simple JavaScript program to join all elements of an array into a string.

let joinArray = (input) => {
  return input.join("+");
};

console.log(joinArray(["Suyash", "Pradhan", "is", "a", "noob"]));
